import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  backgroundContainer: {
    width: "100%",
    height: "100%"
  },
  title: {
    marginBottom: 20
  },
  titleImage: {
    left: "20%"
  },
  container: {
    alignItems: "center"
  }
});

export default styles;
